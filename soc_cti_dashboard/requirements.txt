# Upper bounds keep the twice-daily Actions run reproducible: with bare `>=`
# every scheduled harvest resolved to whatever had shipped that morning, so an
# upstream major release could break the deploy with no change on our side.
# Bump deliberately, and let the sanity job prove the new floor works.
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.32.0,<1.0
httpx>=0.27.0,<0.29
apscheduler>=3.10.4,<4.0
pydantic>=2.9.0,<3.0
feedparser>=6.0.14,<7.0
python-dateutil>=2.9.0,<3.0
aiosqlite>=0.20.0,<1.0
